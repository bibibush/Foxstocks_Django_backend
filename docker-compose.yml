version: "3"
services:
  db:
    image: postgres:latest
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: bibu
      POSTGRES_PASSWORD: bibu
      POSTGRES_DB: mydb
    volumes:
      - ./docker/data:/var/lib/postgresql/data